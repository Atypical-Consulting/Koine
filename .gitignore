bin/
obj/
generated/
Generated/
*.user
.vs/
.idea/
*.received.*
.DS_Store
BenchmarkDotNet.Artifacts/

# Node tooling — the root is an npm workspace root (tooling/*, website), so /node_modules/
# and package-lock.json here are real, tracked workspace artifacts, not stray drops.
/node_modules/

# Skill eval scratch — workspace logs/JSON dropped by create-issue skill evals; never source.
.claude/skills/create-issue-workspace/
.superpowers/

# design-sync (claude.ai/design sync for @atypical/koine-ui) — build/verification scratch.
# The durable set (.design-sync/config.json, NOTES.md, conventions.md, previews/, overrides/,
# gen-adapter-entry.mjs) is committed; everything below is regenerated per run.
.ds-sync/
ds-bundle/
.design-sync/sb-reference/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
