bin/
obj/
generated/
Generated/
*.user
.vs/
.idea/
*.received.*
.DS_Store
BenchmarkDotNet.Artifacts/

# Node tooling — the root is an npm workspace root (tooling/*, website), so /node_modules/
# and package-lock.json here are real, tracked workspace artifacts, not stray drops.
/node_modules/

# Skill eval scratch — workspace logs/JSON dropped by create-issue skill evals; never source.
.claude/skills/create-issue-workspace/
.superpowers/
